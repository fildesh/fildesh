#!/usr/bin/env lace

# Define a {heredoc} variable with some text.
$(H heredoc) tag

Check out this document.
It is a string variable with "llo" in it.
This is the end.
$(H heredoc) tag

# Define a {low} variable that contains the string "low".
$(H low)
llo
$(H low)

# Concatenate the above
$(O echo) zec / "hello\nup\njello " $(H heredoc) \
  " d e\nyel" $(H low) \
  "\nfel" $(H low) '\no' "break\nyesbreak" /

grep llo $(X echo) $(O grep)

# Redefine the {heredoc} variable.
$(H heredoc) b
s/llo/cko/
$(H heredoc) b

sed -e $(H heredoc) $(X grep) $(O sed)

$(H script)
{ print "prefix " $0; }
$(H script)

$(O a) awk $(H script) $(X sed)

$(X a) stdout

