# Fildesh Debian Package

From the project root, run the make script:

```shell
./pkg/deb/make.fildesh
```

It should tell you where the resulting Debian package was created.
Install that with `dpkg -i`.

