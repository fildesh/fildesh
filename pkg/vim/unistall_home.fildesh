#!/usr/bin/env fildesh

$(<< common.fildesh)

rm -f \
  "${dst_root}/${ftdetect_filepath}" \
  "${dst_root}/${ftplugin_filepath}" \
  "${dst_root}/${indent_filepath}" \
  "${dst_root}/${syntax_filepath}"
