module(
    name = "fildesh",
    version = "0.1.8",
    compatibility_level = 1,
)

bazel_dep(name = "platforms", version = "0.0.6")

bazel_dep(name = "google_benchmark", version = "1.7.1", dev_dependency = True)

